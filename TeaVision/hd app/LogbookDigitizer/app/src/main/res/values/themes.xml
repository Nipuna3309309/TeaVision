<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LogbookDigitizer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.LogbookDigitizer" parent="Base.Theme.LogbookDigitizer" />

    <style name="TableHeader">
        <item name="android:textColor">#000000</item>
        <item name="android:textStyle">bold</item>
        <item name="android:padding">12dp</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/table_cell_bg</item>

        <item name="android:width">120dp</item>
        <item name="android:height">60dp</item> <item name="android:textSize">14sp</item>
    </style>
</resources>